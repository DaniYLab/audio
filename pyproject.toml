[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "storyforge"
version = "0.1.0"
description = "Automated story-video production pipeline: YouTube audio -> transcript -> knowledge base -> story -> narration -> images -> video"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "typer>=0.12",
    "rich>=13.7",
    "pyyaml>=6.0",
    "structlog>=24.1",
    "tenacity>=8.3",
    "httpx>=0.27",
    "yt-dlp>=2026.8.19",
]

[project.optional-dependencies]
# Local transcription (GPU/CPU). Heavy; install only when needed.
transcription = [
    "whisperx>=3.3",
]
# Local knowledge base stack.
knowledge = [
    "chromadb>=0.5",
    "qdrant-client>=1.12",
    "FlagEmbedding>=1.3",  # BGE-M3 dense+sparse embedder (SF__KNOWLEDGE__EMBEDDING__PROVIDER=bge_m3_local)
]
# Local image-to-video animation (SVD; SF__ANIMATION__PROVIDER=svd_local).
# huggingface-hub must stay <1.0: transformers (whisperx/BGE-M3) rejects 1.x,
# and diffusers<0.34 has no hub 1.x path either.
animation = [
    "diffusers==0.33.1",
    "accelerate>=1.0",
    "imageio>=2.34",
    "imageio-ffmpeg>=0.5",
    "av>=12",
    "huggingface-hub>=0.34,<1.0",
]
# Free TTS via Edge endpoint.
tts = [
    "edge-tts>=6.1",
]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.10",
    "types-pyyaml",
    "fastapi>=0.111",
    "uvicorn>=0.30",
]

[project.scripts]
storyforge = "storyforge.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/storyforge"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "W",       # pycodestyle / pyflakes
    "I",                 # import sorting
    "N",                 # pep8 naming
    "UP",                # pyupgrade
    "B",                 # bugbear
    "C4",                # comprehensions
    "SIM",               # simplify
    "PTH",               # use pathlib
    "RUF",               # ruff-specific
]
ignore = ["E501"]  # line length enforced by formatter where possible

[tool.ruff.lint.isort]
known-first-party = ["storyforge"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
explicit_package_bases = true
disallow_untyped_decorators = false  # typer decorators are untyped

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
    "integration: tests that touch external services or heavy models",
    "e2e: full-pipeline end-to-end test with fake providers (T-E2E gate)",
]
